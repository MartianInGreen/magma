#!/bin/bash

# For testing purposes
# you can append "python3", "nodejs", "bash", "fish" or any other command to the end of the docker run command

docker run -it -w "/home/codeapi" --rm --name codeapi codeapi